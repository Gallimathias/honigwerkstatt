import { RailNavDirective } from './rail-nav.directive';

describe('RailNavDirective', () => {
  it('should create an instance', () => {
    const directive = new RailNavDirective();
    expect(directive).toBeTruthy();
  });
});
